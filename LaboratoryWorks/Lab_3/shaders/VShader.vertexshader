#version 330 core

layout(location = 0) in vec3 mPos;
layout(location = 1) in vec3 mColor;

uniform mat4 MVP;
out vec3 FragmentColor;

void main(){
    gl_Position = MVP*vec4(mPos,1);
    FragmentColor = mColor;
}

