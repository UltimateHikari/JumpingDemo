#version 330 core

out vec3 color;
in vec3 fColor;
in vec2 TexCoords

uniform sampler2D texture_diffuse1;

void main()
{
	//color = vec3(1,0,0);
	color = fColor;
	//color = texture(texture_diffuse1, TexCoords);
}