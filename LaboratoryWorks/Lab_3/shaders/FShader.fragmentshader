#version 330 core

out vec4 color;
in vec3 fColor;
in vec2 fTexCoords;

uniform sampler2D texture_diffuse1;

void main()
{
	//color = fColor;
	color = texture(texture_diffuse1, fTexCoords);
}